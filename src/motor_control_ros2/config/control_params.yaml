motor_control_node:
  ros__parameters:
    control_frequency: 200.0  # 控制循环频率（Hz），同时决定 DJI 电机命令帧发送频率
    command_timeout: 0.5      # 上层话题超时（s），超过此时间未收到 dji_motor_command 则清零输出
    config_file: "config/motors.yaml"  # 电机硬件映射配置文件路径（相对 package share 目录）
